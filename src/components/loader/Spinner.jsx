import "./spinner.css";

const Spinner = () => {
  return (
    <section className="flex justify-center items-center">
      <span className="loader"></span>
    </section>
  );
};

export default Spinner;
